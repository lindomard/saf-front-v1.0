export interface WhatsAppEnviarArquivo {
    idPessoa?: string,
    sessionId?: string,
    nomeArquivo?: string,
    foneDest?: string,
    extensaoArq?: string,
    tamanhoArquivo?: number,
    status?: number
}